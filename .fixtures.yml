fixtures:
  symlinks:
    graphite: "#{source_dir}"
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref:  "4.5.1"
    docker_systemd:
      repo: "ajsmith/docker_systemd"
      ref:  "0.3.1"
